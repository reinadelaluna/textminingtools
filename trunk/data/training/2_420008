You could check out <a href="http://www.gnome.org/projects/memprof/" target="_blank">memprof</a>; it looks promising.Hi,<br />
<br />
     I want to know some freeware memory leak tools that are in GUI format. Can any one suggest any GUI memory leak tool? I am currently using valgrind, which is not a GUI tool. I want one GUI tool which is very good in detecting memory leaks.